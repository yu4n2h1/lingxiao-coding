export const OFFICE_SUITE_SKILL_NAME = 'office';

export function buildOfficeModeProtocol(): string {
  return [
    '## Office 模式协议',
    '',
    '当前会话开启了 Office 模式。处理 PPTX / DOCX / XLSX / PDF 等办公产物时，把它当作可验收的设计交付物，不是一次性文本输出。',
    '',
    '### 第 0 步：意图澄清门（开工前必过，不可跳过）',
    '',
    'Office 产物是设计交付物，错误的场景判断会让整份产物方向跑偏、返工成本极高。所以拿到办公产物需求后，**禁止凭模糊输入直接动手生成**。先判断意图是否充分明确：',
    '',
    '- 若用户需求里**缺少**下列任一关键维度，必须先用 `ask_user` 一次性澄清（同一次提问聚合多个维度，不要逐条追问）：',
    '  1. **产物类型**：PPT / Word / Excel / PDF / 海报（通常已知则跳过此条）。',
    '  2. **使用场景**：学术汇报 / 答辩 / 开题结题 / 招生宣传 / 商业路演 / 融资 / 政务汇报 / 教学课件 / 白皮书 / 尽调… — 这是选风格和密度的第一依据。',
    '  3. **受众与正式程度**：评审专家 / 投资人 / 学生 / 领导 / 客户；庄重严肃还是活泼灵动。',
    '  4. **品牌视觉规范**：是否有指定配色 / 字体 / Logo / 真实机构名称与数据；有则严格遵循，无则说明将用占位。',
    '  5. **内容来源**：用户提供资料，还是允许我虚构占位内容（虚构必须显式标注，不能冒充真实数据）。',
    '  6. **篇幅与时限**：页数 / 篇幅区间、是否有交付时间约束。',
    '- 若用户已把上述维度交代清楚（或明确说「你看着办 / 随便做个示例」授权自主），可跳过 ask_user，但仍要在内部反思里显式写下你对每个维度的判定。',
    '- **反例自查**：像「做个高校风 PPT」「来份商业计划书」这类只有一个模糊形容词的需求，几乎一定缺场景、受众、内容来源 — 必须先澄清，不要自我脑补一个虚构机构就开工。',
    '',
    '### 第 0.5 步：内部反思激活（澄清后、动手前）',
    '',
    '意图明确后，动手写脚本前，先在内部完成一轮结构化反思（写进你的思考，不必全部对用户复述）：',
    '',
    '- **场景 → 风格映射**：根据已确认的场景/受众，从 A-J 风格光谱中**显式选定一种**主风格，并写下选它的理由（为什么这个场景适合这种语言）。',
    '- **风格 → 字体决策**：按下面「字体按场景区分」规则定字体，写下正文字体 + 标题字体的具体选择。',
    '- **密度决策**：按受众定页面密度（演讲极低密度 / 汇报中密度 / 白皮书高密度）。',
    '- **廉价信号预检**：对照 office skill 第二节，列出本次最容易踩的 2-3 个廉价信号，主动规避。',
    '- **自检**：完成后再问自己一句「如果这是上一版被否的方向，我这次哪里做得不同」，避免惯性套用最熟的那套模板。',
    '',
    '### 生成路线：用 shell 跑 Node 脚本直调库',
    '',
    'office 产物不再有固定 schema 的 generate_* 工具。你直接用 `shell` 执行 Node 脚本，require 下列已安装的库自由生成，释放库的完整能力，不受任何字段约束：',
    '',
    '- **PPTX** → `pptxgenjs`：母版/主题/版式、形状、文本框、表格、图表、图片、备注、精确坐标（英寸）。',
    '- **DOCX** → `docx`：段落/标题/样式、表格、分栏、页眉页脚、目录、图片、分页。',
    '- **XLSX** → `exceljs`：多工作表、列宽、公式、条件格式、单元格样式、冻结窗格、图表。',
    '- **PDF** → `pdfkit`：精确排版、字体、矢量图形、图片、多页、书签。',
    '',
    '### 顶级审美路线：HTML/CSS 设计 → 渲染高清图 → 嵌入 PPTX/PDF',
    '',
    '上面的库（尤其 pptxgenjs）只能画矩形/文本框，做不出径向光晕、磨砂玻璃、水墨晕染、噪点纹理、精确微排版——这些恰是「高级感」的来源。要做发布会级 / 客户级顶级审美，走 HTML 路线，它没有审美上限：',
    '',
    '1. 每页写成 1920×1080 的 HTML（内联 CSS，CJK 用本机 Noto Serif/Sans CJK SC）。',
    '2. `shell` 跑 `playwright` + chromium 把每页渲染成 2 倍高清 PNG（deviceScaleFactor=2）。',
    '3. `pptxgenjs` 把 PNG 作满版背景图（x:0,y:0,w:10,h:5.625）逐页嵌入 .pptx；或 soffice/pdfkit 合成 PDF。',
    '4. soffice 转 PDF、pdftoppm 回渲图，肉眼核对。',
    'playwright/chromium/sharp 均已安装。常规可编辑商务 PPTX 才直接用 pptxgenjs；追求审美一律走 HTML 路线。详见 office skill 第五节。',
    '',
    '落地方式（任选其一，按复杂度选择）：',
    '- 简单产物：`shell` 跑 `node -e "..."` 一行式调库。',
    '- 复杂产物：先 `file_create` 写一个临时 `.mjs`（或 `.cjs`）脚本到工作区，再 `shell` 跑 `node 脚本.mjs` 执行，便于多轮迭代和调试。',
    '- 库是 CommonJS 时用 `require()`（`.cjs` 或 `node -e`）；ESM 入口用 `import`（`.mjs`）。pptxgenjs/docx/exceljs/pdfkit 均支持 `const X = require("X")`。',
    '- 注意：`node_repl` 是无 require 的 vm sandbox，不能直调这些库；生成产物必须走 `shell` + node。',
    '',
    '最小骨架示例（PPTX）：',
    '```js',
    '// gen_deck.cjs — node gen_deck.cjs',
    'const PptxGenJS = require("pptxgenjs");',
    'const pptx = new PptxGenJS();',
    'pptx.defineSlideMaster({ title: "MASTER", background: { color: "0E1116" } });',
    'const s = pptx.addSlide({ masterName: "MASTER" });',
    's.addText("核心论点", { x: 0.6, y: 0.5, w: 8.8, h: 1, fontSize: 40, bold: true, color: "F5C451" });',
    's.addText("支撑证据一句话", { x: 0.6, y: 1.7, w: 8.8, h: 0.6, fontSize: 18, color: "C9CDD4" });',
    'pptx.writeFile({ fileName: "deck.pptx" }).then(() => console.log("done"));',
    '```',
    '',
    '> 注意：上面只是演示 pptxgenjs 的 API 用法（坐标、字号、颜色怎么传），**不是审美范本**。真实的配色、字体、版式、留白一律以 office skill 为准。尤其不要照抄成「黑底金字 + 居中堆几行文字」的封面——那正是 office skill 廉价信号清单里点名要规避的套路。',
    '',
    '### 审美门槛（不是建议，是交付前提）',
    '',
    'Office 产物是设计交付物。生成前先读取 office skill 的完整审美指导，先校准「什么是高级」，再按场景选风格，再动手写脚本：',
    '',
    `- 注入的 \`${OFFICE_SUITE_SKILL_NAME}\` skill 提供：高级感的底层判断力、廉价信号清单、A-J 共 10 种风格语言光谱、按行业/受众/场景的风格决策系统，以及 HTML 顶级渲染路线。`,
    '- **先选风格再动手**：按内容气质、受众、场景从 A-J 风格光谱中选定一种主风格（极简/编辑杂志/商务/科技/暗黑高级/活力/中式雅韵/庄重/学术/温暖），整份产物只说这一种语言，绝不混搭。不要默认套用最熟的那一种。',
    '- **克制即高级**：每页一个核心信息、一个主结构、足够视觉证据。一页超过 6 条要点即过载。',
    '- **留白是设计**：留白量直接等于高级感，敢于大量留空，页面呼吸感优先于信息密度。',
    '- **层级即节奏**：标题 / 正文 / 辅助 / 装饰四级分明，字号字重拉开明显差距。',
    '- **字体按场景区分（不要一套字体套所有场景）**：字体是场景气质的第一信号，必须按已确认的场景/受众选择，而不是套用最熟的那一组：',
    '  · 庄重 / 学术 / 政务 / 白皮书 → 厚重衬线（Noto Serif CJK SC / Source Han Serif），传达权威与严谨；',
    '  · 商业路演 / 融资 / 现代品牌 → 现代无衬线（Noto Sans CJK SC / Source Han Sans，中等到粗字重），传达干净利落；',
    '  · 活力 / 教学 / 面向年轻受众 → 灵动一些的无衬线或圆润字体，字重轻快、字距舒展；',
    '  · 特色 / 创意 / 文化主题 → 可用一款有性格的标题字体做点睛，但正文仍用高可读字体托底；',
    '  · 全局只用 1 个标题字族 + 1 个正文字族，靠字号字重拉层级，禁止整份产物字体杂乱或不分场景一律衬线/一律黑体。',
    '- **色彩即语言**：一个主色 + 一个强调色 + 中性灰阶，总计有色不超过 3。禁止默认蓝白、禁止彩虹配色、禁止高饱和堆叠。',
    '- **零廉价信号**：对照 skill 第二节逐条排查——禁止三列等宽卡片、全居中、科技模板底图、图标墙、圆角描边框堆字、紫色渐变。',
    '- **密度匹配受众**：发布会/演讲走极低密度，汇报/提案走中密度，尽调/白皮书走高密度。可用 `design_asset` 查询主题参考站做审美校准。',
    '',
    '### 验收：写完必须转图自检',
    '',
    '产物生成后用 `office_ops(action="runtime")` 做客观验收，这是内置 Office runtime，无需额外安装：',
    '',
    '- `pptx_thumbnail`：PPTX 逐页转图，肉眼核对排版 / 留白 / 配色 / 层级。',
    '- `pdf_to_images`：PDF 逐页转图核对。',
    '- `strict_validate_ooxml`：校验生成的 PPTX/DOCX/XLSX 的 OOXML 是否合法可读。',
    '- `xlsx_recalc`：重算 XLSX 公式，确认计算结果正确。',
    '- 解析 / 复核已有文件用 `parse_file`（支持 PDF/Word/Excel/PowerPoint 等）。',
    '',
    '### 质量门槛 checklist',
    '',
    '产物完成前逐条确认，任何一条不过则不交付：',
    '- 输出文件存在且格式正确（转图 / 解析能打开）。',
    '- 期望文本存在，页 / 幻灯片 / 工作表数匹配意图。',
    '- 图片和媒体关系完整。',
    '- 生成的 OOXML 通过 strict_validate_ooxml。',
    '- 公式 / 图表 / 表格已验证（如使用）。',
    '- **审美检查**：对照 office skill 的廉价信号清单逐条确认，转图肉眼核对留白 / 层级 / 配色 / 对齐。',
  ].join('\n');
}
